# Exemplo de configuração (.env.example)
# Copie para um arquivo chamado .env e preencha sua chave

# Diretórios (opcionais — pode sobrescrever via CLI)
BRONZE_COUNTRIES=data/bronze/countries
SILVER_COUNTRIES=data/silver/countries
BRONZE_RATES=data/bronze/rates
SILVER_RATES=data/silver/rates
SILVER_ENRICHED=data/silver/enriched

# Provedor de câmbio:
# - apilayer   → requer EXCHANGERATE_API_KEY
# - frankfurter → grátis, sem chave
EXCHANGE_PROVIDER=apilayer
EXCHANGERATE_API_KEY=YOUR_API_KEY_HERE
HTTP_TIMEOUT=30